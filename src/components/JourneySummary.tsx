export function JourneySummary({onSave,onRestart,onHome,saved}:{onSave:()=>void;onRestart:()=>void;onHome:()=>void;saved:boolean}) { return <section className="center-screen summary"><div className="horizon-mark"/><p className="eyebrow">Rive provisoire</p><h1>Une rive apparaît.</h1><p>Prenez un instant pour laisser cette traversée se déposer.</p><div className="summary-actions"><button className="primary" onClick={onSave} disabled={saved}>{saved?'Traversée conservée':'Conserver cette traversée'}</button><button className="secondary" onClick={onRestart}>Recommencer</button><button className="quiet" onClick={onHome}>Revenir à l’accueil</button></div></section> }
