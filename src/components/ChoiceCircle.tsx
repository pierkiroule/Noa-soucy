export interface CircleOption { id:string; title:string; symbol:string; description?:string }
export function ChoiceCircle({question,options,selected,onToggle,multiple=false,max=1}:{question:string;options:CircleOption[];selected:string[];onToggle:(id:string)=>void;multiple?:boolean;max?:number}) { return <div className="choice-stage"><p className="step-label">Préparer la traversée</p><h2>{question}</h2>{multiple&&<p className="choice-hint">Jusqu’à {max} ressources · {selected.length}/{max}</p>}<div className="choice-orbit">{options.map((option,index)=><button type="button" style={{'--i':index,'--n':options.length} as React.CSSProperties} className={`orbit-choice ${selected.includes(option.id)?'selected':''}`} key={option.id} onClick={()=>onToggle(option.id)} aria-pressed={selected.includes(option.id)}><span>{option.symbol}</span><strong>{option.title}</strong>{option.description&&<small>{option.description}</small>}</button>)}</div></div> }
