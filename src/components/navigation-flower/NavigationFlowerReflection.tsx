export function NavigationFlowerReflection({ value, onChange, onFinish }: { value: string; onChange: (value: string) => void; onFinish: () => void }) { return <section className="flower-reflection"><span className="eyebrow">Petit geste final</span><h1>Quel petit geste pourrait aider ce pétale à s’ouvrir un peu cette semaine ?</h1><textarea value={value} onChange={event => onChange(event.target.value)} aria-label="Petit geste facultatif" placeholder="Un mot, une phrase, ou rien du tout…"/><button className="primary" onClick={onFinish}>Terminer ma traversée</button><small>Ce champ est facultatif. Vos choix restent uniquement sur cet appareil.</small></section> }
