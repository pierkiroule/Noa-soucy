/**
 * Conservé pour ne pas casser d'éventuels imports historiques.
 * Le parcours NFC ne monte plus ce composant : une trace ne se crée qu'après le conte.
 */
export function NaoPasserOnboarding(_props: { nutId: string; onContinue: () => void }) {
  return null
}
