export function ResonanceSurfaceControls({ muted, canFinish, onExit, onRestart, onToggleMuted, onFinish }: { muted: boolean; canFinish: boolean; onExit: () => void; onRestart: () => void; onToggleMuted: () => void; onFinish: () => void }) { return <div className="resonance-controls"><button onClick={onExit}>Quitter</button><button onClick={onRestart}>Recommencer</button><button onClick={onToggleMuted}>{muted ? 'Son coupé' : 'Son activé'}</button>{canFinish && <><button onClick={onRestart}>Poursuivre encore</button><button className="primary" onClick={onFinish}>Terminer ma traversée</button></>}</div> }
