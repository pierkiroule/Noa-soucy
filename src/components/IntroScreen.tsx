import type { StoryMetadata } from '../types/story'
export function IntroScreen({ metadata, canResume, onStart, onResume, onRestart }: { metadata: StoryMetadata; canResume: boolean; onStart: () => void; onResume: () => void; onRestart: () => void }) { return <main className="screen intro"><p className="eyebrow">Conte textuel</p><h1>{metadata.title}</h1><p className="subtitle">{metadata.subtitle}</p><p className="invitation">Une histoire à lire, à traverser et à laisser résonner.</p><div className="controls centered">{canResume ? <><button className="button" onClick={onResume}>Reprendre</button><button className="button secondary" onClick={onRestart}>Recommencer</button></> : <button className="button" onClick={onStart}>Commencer le conte</button>}</div></main> }
