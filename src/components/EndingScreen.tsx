import type { EndingModule } from '../types/story'
export function EndingScreen({ ending, onReview, onRestart, onHome }: { ending: EndingModule; onReview: () => void; onRestart: () => void; onHome: () => void }) { return <main className="screen ending"><p className="eyebrow">Au bord d’un autre horizon</p><h2>{ending.title}</h2><div className="story-text">{ending.text}</div><div className="controls centered"><button className="button secondary" onClick={onReview}>Revoir le conte</button><button className="button" onClick={onRestart}>Recommencer</button><button className="text-button" onClick={onHome}>Revenir à l’accueil</button></div></main> }
