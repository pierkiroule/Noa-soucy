export interface Vec2 { x: number; y: number }
export interface FlyingBubbleState { id: string; position: Vec2; velocity: Vec2; radius: number; rotation: number; rotationSpeed: number; opacity: number }
