import { useEffect,useMemo } from 'react';import { audioEngine } from './AudioEngine';import { getActiveEffects } from './engine';import { resolveSceneMedia } from './MediaResolver';import { SceneStage } from './SceneStage';import { useStoryState } from './StoryState';import type { StoryScene } from './types'
export function ScenePlayer({scene}:{scene:StoryScene}){const{state,next,togglePause,setMedia}=useStoryState();const effects=useMemo(()=>getActiveEffects(scene.effects,state.currentSceneTime),[scene.effects,state.currentSceneTime]);useEffect(()=>{let active=true;void resolveSceneMedia(scene).then(media=>{if(active)setMedia(media)});void audioEngine.load(scene.media.audio).then(()=>audioEngine.play());return()=>{active=false;audioEngine.stop()}},[scene,setMedia]);useEffect(()=>{if(state.currentSceneTime>=scene.duration)next()},[state.currentSceneTime,scene.duration,next]);useEffect(()=>{if(state.isPaused)audioEngine.pause();else audioEngine.resume()},[state.isPaused]);const fallback=()=>setMedia({kind:'placeholder',fallbackSceneId:scene.media.fallbackSceneId},'Le média vidéo est illisible.');return <section className={`scene transition transition--${scene.transitionIn}`}><SceneStage scene={scene} media={state.media} parameters={state.parameters} effects={effects} time={state.currentSceneTime} onMediaError={fallback}/><div className="scene__copy"><span>Scène · {scene.title}</span><p>{scene.text}</p></div><button className="scene__pause" onClick={togglePause}>{state.isPaused?'Reprendre':'Pause'}</button></section>}
