import type { ActiveEffect, EffectCue, StoryDefinition, StoryPause, StoryResponses, StoryRuntimeState, StoryVisualParameters } from './types'
export function getActiveEffects(effects:EffectCue[],currentTime:number):ActiveEffect[]{return effects.filter(c=>currentTime>=c.start&&currentTime<c.start+c.duration).map(c=>{const progress=Math.min(1,Math.max(0,(currentTime-c.start)/c.duration));return {...c,progress,effectiveIntensity:(c.intensity??1)*Math.sin(progress*Math.PI)}})}
export function resolvePauseParameters(pause:StoryPause,selectedIds:string[]):Record<string,number|string|boolean>{const allowed=new Set(selectedIds.slice(0,pause.maxChoices));return pause.options.filter(o=>allowed.has(o.id)).reduce((all,o)=>({...all,...o.params}),{})}
export function buildStoryParameters(definition:StoryDefinition,responses:StoryResponses):StoryVisualParameters{return definition.steps.filter((step):step is StoryPause=>step.type==='pause').reduce((all,pause)=>({...all,...resolvePauseParameters(pause,responses[pause.id]??[])}),{fogDensity:.1,waveIntensity:.25,darkness:0,horizontalDrift:.2,lightIntensity:.6,growthSpeed:1,stability:.5,windIntensity:.25})}
export function createInitialState(storyId:string):StoryRuntimeState{return{storyId,currentStepIndex:0,currentSceneTime:0,isPlaying:false,isPaused:false,responses:{},parameters:{},progress:0,started:false,completed:false}}
