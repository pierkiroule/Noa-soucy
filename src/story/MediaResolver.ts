import type { ResolvedSceneMedia, StoryScene } from './types'
export type MediaProbe=(url:string)=>Promise<boolean>
export const browserMediaProbe:MediaProbe=async url=>{try{const response=await fetch(url,{method:'HEAD'});if(!response.ok)return false;const contentType=response.headers.get('content-type')??'';const contentLength=Number(response.headers.get('content-length')??1);return contentLength>0&&(contentType.startsWith('video/')||contentType.includes('octet-stream'))}catch{return false}}
export async function resolveSceneMedia(scene:StoryScene,probe:MediaProbe=browserMediaProbe):Promise<ResolvedSceneMedia>{if(!scene.media.video)return{kind:'placeholder',fallbackSceneId:scene.media.fallbackSceneId};try{if(await probe(scene.media.video))return{kind:'video',source:scene.media.video,fallbackSceneId:scene.media.fallbackSceneId}}catch{/* A custom probe must never stop the story. */}const warning=`Média indisponible pour ${scene.id}; utilisation du placeholder.`;console.warn(warning);return{kind:'placeholder',fallbackSceneId:scene.media.fallbackSceneId,warning}}
